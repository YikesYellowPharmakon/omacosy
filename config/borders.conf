# omacosy-borders — ring styling. Reload: it re-reads on file change.
width=2
# macOS 27 window chrome is ~16pt. Sequoia's 10pt ring sat inside the
# curve and read as a second, tighter frame.
gap=0
radius=16
radius:Finder=16
radius:Safari=16
radius:Google Chrome=16
radius:Ghostty=14
radius:Cursor=16
radius:WhatsApp=16
